fastapi
uvicorn
openai
httpx
python-dotenv

# LangChain: orchestrates the LLM calls (ChatOpenAI, pointed at DeepSeek's
# OpenAI-compatible endpoint) and the retrieval building blocks (FAISS
# vector store, BM25Retriever, HuggingFaceEmbeddings, PromptTemplate).
# See rag.py's module docstring for which parts are still hand-rolled
# (parent/child chunking, RRF + per-parent score aggregation, TheMealDB
# fallback) and why.
langchain
langchain-core
langchain-openai
langchain-community
langchain-huggingface

faiss-cpu
rank-bm25
sentence-transformers

# sentence-transformers pulls in torch; pin the CPU-only wheel explicitly
# (via PyTorch's own index) so deploys don't accidentally resolve a much
# larger CUDA build that isn't needed for this project's scale.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
